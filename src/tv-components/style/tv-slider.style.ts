import { themeStyle } from "./tv-theme.style";

export let sliderStyle = {
    shadow: "0 2px 6px 5px rgba(0,0,0,.5)",
    openAnimation: "500ms ease-out",
    closeAnimation: "500ms ease-in",
    defaultWidth: "38%",
    defaultPosition: "right",
    backgroundColor: themeStyle.primaryDark,
    defaultOpen: true,
    title: {
        height: "128px"
    }
}